# connect to the server as the MySQL root user:
shell> mysql --user=root mysql
$ mysql -u root -p

# create database and let user use it.
$ mysql -u root -p
mysql> CREATE DATABASE CogentInterviewDB;
mysql> grant usage on *.* to gengwg@localhost;
mysql> grant all privileges on CogentInterviewDB.* to gengwg@localhost;

# show details of table
SELECT * FROM table_name;

# add primary key to table
mysql> ALTER TABLE ITEM
    -> ADD PRIMARY KEY (Item_Id);

# display a list of all databases under MySQL Server
show databases;

# display a list of all tables under a database
show tables;

# select a database for use
use testDB;

# to look at the structure of the orders table:
describe orders;

# delete a table
drop table item_details;

# Comment
"#", 
"-- " (note space), 
"/* .... */"
