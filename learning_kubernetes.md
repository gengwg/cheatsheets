### Container

a collection of software processes unified by one namespace, with access to an operating system kernel that it shares with other containers, and little to no access between containers.

a runtime instance of a docker image contains 3 things:

- a docker image
- an execution environment
- a standard set of instructions

two features that allows k8s to scale: registration; discovery.


