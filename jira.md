
- `c`: create issue
- open commandline pallete: `cmd + k`
- In task mode, '/' goes to search
  - 'my' all my issues
  - '<Pjr name> shows all team issues

JQL: status WAS "In Progress"

'KAN Board' --> 'View Settings' --> Toggle 'Open work items in sidebar'

Go to personal boards created from filters:

Click on your user from top right corner --> Click on 'Jira/Your boards'

TIP: VS Code has Atlassian plugin!

### Slack Jira Bot


`/jira create Task Title Summary ` pops up a window to fill out a task.

Pro Tip: First run `/jira connect` to set default Project and Issue Type.

### Time tracking settings

`cog wheel` next to profile --> `Work items` in `Jira admin settings` --> `Time tracking` in `Work item features`

